{
  "$schema": "https://modelcontextprotocol.io/schemas/mcp-client.json",
  "mcpServers": {
    "aceleriq": {
      "transport": "streamable-http",
      "url": "${ACELERIQ_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${ACELERIQ_MCP_TOKEN}",
        "Accept": "application/json, text/event-stream",
        "X-Agent-Origin": "${ACELERIQ_AGENT_ORIGIN:-codex}"
      },
      "description": "Aceleriq Performance OS — MCP central (leitura + escrita restrita + Segundo Cérebro).",
      "roots": []
    }
  }
}
